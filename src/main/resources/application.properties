spring.application.name=ShopWise
server.port=${PORT:5000}
#spring.datasource.url=jdbc:postgresql://localhost:5432/shopwise
spring.datasource.url=jdbc:postgresql://aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pool_mode=transaction
spring.datasource.username=postgres.fxwxhhhqlkpslmnjkqso
spring.datasource.password=ZKiwFL0hzhwi0b9G
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.resetOnReturn=prepared_statements

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Gmail SMTP configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.jpa.hibernate.ddl-auto=update
debug=true

# Multipart file upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Gemini API Configuration
gemini.api.key=${GEMINI_API_KEY}
gemini.model.name=${GEMINI_MODEL_NAME}
gemini.project.id=${GEMINI_PROJECT_ID}
gemini.location.id=us-central1

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
